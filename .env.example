# Optional: used for smarter cover letters and screening answers.
# Without this key, the app uses high-quality templates based on your CV.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Keep private application data outside version control.
APPLYPILOT_PROFILE=data/profile.local.json
APPLYPILOT_CV_PATH=
